import Link from "next/link";

const RegisterPage = () => {
    return (
        <div>
            Sudah punta akun <Link href={"/login"}>Login</Link>
        </div>
    )
};

export default RegisterPage;
